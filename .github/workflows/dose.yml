name: blank

on:
  push:

jobs:
  liberate:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        factor: ["coach", "fly", "enclose", "submerge", "awkward"]
        laundry: ["organism", "vocabulary", "industrial", "Mediterranean"]
    steps:
    - uses: actions/checkout@v6
    - uses: actions/setup-java@v5
      with:
        distribution: 'zulu'
        java-version: '25'
    - run: java -jar Et
